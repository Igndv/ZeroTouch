1. All the commands should be run in the virtual environment.
2. All scripts cannot be run in the agent, just show me the steps and ill run the command myself.
3. Ask user for context if you are unsure about my prompt, don't assume anything.
4. For every time making a code/file, make sure to make a documentation (place it inside the documentation folder)in a seperate file (you can group the documentation by the feature, for example: gesture_recognition.md, voice_recognition.md, etc.)